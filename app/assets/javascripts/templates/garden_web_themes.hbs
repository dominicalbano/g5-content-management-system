<div class="theme-picker thumbnail-field clearfix">
  <div class="l-container">
    <div class="toggle-content page-section">
      <div class="page-title"><h2>Select a Theme</h2></div>
      {{#if model.isUpdating}}
        <div class="loading">Loading...</div>
      {{else}}
        {{#view App.ThumbnailScrollerView}}
          <div class="jTscrollerContainer">
            <div class="jTscroller thumbs clearfix" style="width:100%">
              {{#each model}}
                <div class="thumb">
                  <a href="#" {{ action "update" this }}>
                    {{#view App.GardenWebThemeView contentBinding="this"}}
                    <img alt="Thumbnail" {{ bind-attr src="thumbnail" }} {{ bind-attr class="view.isSelected" }}>
                    {{ name }}
                    {{/view}}
                  </a>
                </div>
              {{/each}}
            </div>
          </div>
          <a href="#" class="jTscrollerPrevButton"></a>
          <a href="#" class="jTscrollerNextButton"></a>
        {{/view}}
      {{/if}}
      {{ render "webTheme" }}
    </div>
  </div>
</div>
