{{#if view.content.thumbnail }}
  {{! thumbnails are set on the view if used, but name is inherited from controller}}
  <img alt="Thumbnail" {{ bind-attr src="view.content.thumbnail" }} />
  {{ view.content.name }}
{{else}}
  <img alt="Thumbnail" {{ bind-attr src="thumbnail" }} />
  {{ name }}
{{/if}}
