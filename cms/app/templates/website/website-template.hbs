<div>
  <div class="l-cell">
    <img alt="Thumbnail" class="thumb" {{ bind-attr src="webLayout.thumbnail" }} />
  </div>

  <div class="l-cell">
    <img alt="Thumbnail" class="thumb" {{ bind-attr src="webTheme.thumbnail" }} />
  </div>

  <div class="l-cell l-double-cell">
    <h3 class="template-heading">Widgets</h3>
    <ul class="widgets widgets--small">
      <!-- refactor this -->
      {{#each logoWidgets}}
        <li>
          {{ partial "website/widget" }}
        </li>
      {{/each}}
      {{#each btnWidgets}}
        <li>
          {{ partial "website/widget" }}
        </li>
      {{/each}}
      {{#each navWidgets}}
        <li>
          {{ partial "website/widget" }}
        </li>
      {{/each}}
      {{#each asideBeforeMainWidgets}}
        <li>
          {{ partial "website/widget" }}
        </li>
      {{/each}}
      {{#each asideAfterMainWidgets}}
        <li>
          {{ partial "website/widget" }}
        </li>
      {{/each}}
      {{#each footerWidgets}}
        <li>
          {{ partial "website/widget" }}
        </li>
      {{/each}}
    </ul>
  </div>
</div>
