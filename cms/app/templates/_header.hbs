<div class="breadcrumb">
  <div class="l-container">
    <nav class="nav" role="navigation">
      <ul>
        <li><a href="/">{{controllers.client.model.name}}</a></li>

        {{#if controllers.website.model }}
          <li>
            &raquo;
            {{#link-to "website" controllers.website}}
              {{controllers.website.model.name}}
            {{/link-to}}
          </li>
        {{/if}}

        {{#if controllers.webPageTemplate.model}}
            &raquo;
            <a>{{controllers.webPageTemplate.model.name}}</a>
        {{/if}}
      </ul>
    </nav>
  </div>
</div>
